const schedulerUpdateTranslations = {
    'Scheduler.Update.ScheduledProcess.Success': 'Process {{ processName }} schedule updated',
    'Scheduler.Update.ScheduledProcess.Failed': 'Failed to update process {{ processName }} schedule',
};

export default schedulerUpdateTranslations;

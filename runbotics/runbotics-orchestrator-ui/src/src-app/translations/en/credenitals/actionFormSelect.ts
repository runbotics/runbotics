const actionFormSelect = {
    'Credentials.ActionFormSelect.Form.Section.Title': 'Credential',
    'Credentials.ActionFormSelect.Form.CustomCredential.True': 'CUSTOM',
    'Credentials.ActionFormSelect.Form.CustomCredential.False': 'PRIMARY',
    'Credential.ActionFormSelect.Dialog.ResetToPrimary.Label': 'Reset to primary',
    'Credential.ActionFormSelect.Dialog.ResetToPrimary.Info': 'If you reset to primary, the credential from the top of the list in CONFIGURE tab will be used in this action',
    'Credential.ActionFormSelect.Dialog.SetToPrimary.Label': 'Primary applied',
    'Credential.ActionFormSelect.Dialog.SetToPrimary.Info': 'Action will use the credential from the top of the list in CONFIGURE tab',
    'Credential.ActionFormSelect.Dialog.NoCredentialsSpecified': 'No credentials specified in CONFIGURE tab',
};

export default actionFormSelect;

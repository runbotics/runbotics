import React, { FC } from 'react';

import Layout from '#src-landing/components/Layout';
import PrivacyPolicyView from '#src-landing/views/PrivacyPolicyView';

const PrivacyPolicyPage: FC = () => (
    <Layout>
        <PrivacyPolicyView />
    </Layout>
);

export default PrivacyPolicyPage;

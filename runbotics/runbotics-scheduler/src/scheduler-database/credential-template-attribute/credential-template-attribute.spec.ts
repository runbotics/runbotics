import { CredentialTemplateAttribute } from './credential-template-attribute.entity';

describe('CredentialTemplateAttribute', () => {
  it('should be defined', () => {
    expect(new CredentialTemplateAttribute()).toBeDefined();
  });
});

const general = {
    'Credentials.Collections.Page.Title': 'Credential Collections',
    'Credentials.Tab.Collections': 'Collections',
    'Credentials.Tab.Credentials': 'Credentials',
    'Credentials.GoBack': 'Back to all credentials'
};

export default general;

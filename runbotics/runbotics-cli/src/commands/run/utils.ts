import { join } from 'path';

export const SCHEDULER_RELATIVE_PATH = join('runbotics', 'runbotics-scheduler');
export const UI_RELATIVE_PATH = join('runbotics', 'runbotics-orchestrator-ui');
export const BOT_RELATIVE_PATH = join('runbotics', 'runbotics-desktop');
export const API_RELATIVE_PATH = join('runbotics-orchestrator');
export const LP_RELATIVE_PATH = join('runbotics', 'runbotics-lp');
export const PROXY_RELATIVE_PATH = join('runbotics', 'runbotics-proxy');

export enum BrowserScrollPageMode {
    SMOOTH = 'smooth',
    INSTANT = 'instant',
}

export enum BrowserScrollPagePosition {
    TOP = 'top',
    BOTTOM = 'bottom',
    ELEMENT = 'element',
    HEIGHT = 'height',
}
